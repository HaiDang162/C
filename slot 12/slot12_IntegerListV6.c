#include <stdio.h>
#include <stdlib.h>

// vong lap vo tan

int main(int argc, char *argv[]) {
	for (int i = 1; 1 <= 1; i++)
	{
		printf("%d ", i);
		if (i == 100)
			break; // dk thoat lap trong for
	}
	return 0;
}