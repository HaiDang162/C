#include <stdio.h>
#include <stdlib.h>

// in ra cac con so tu 1..100

int main(int argc, char *argv[]) {
	printf("The list of integers from 1..100\n");
	for (int i = 1; i <= 100; i++)
		printf("%d ", i);
	return 0;
}